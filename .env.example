# Postgres
POSTGRES_USER=alma
POSTGRES_PASSWORD=alma
POSTGRES_DB=alma

# Upload constraints
MAX_UPLOAD_MB=10

# NOTE: DATABASE_URL and UPLOAD_DIR are set for the api service in
# docker-compose.yml. Override here only if running the backend outside Docker.
